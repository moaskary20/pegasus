# 🧪 اختبار سريع - تحقق من أن كل شيء يعمل

---

## ✅ نقاط الاختبار

### 1️⃣ اختبر قاعدة البيانات

```bash
php artisan tinker

# اختبر 1: تحقق من الجدول موجود
>>> Schema::hasTable('zoom_meetings')
# النتيجة المتوقعة: true ✅

# اختبر 2: عد الأعمدة
>>> Schema::getColumns('zoom_meetings') |> count()
# النتيجة المتوقعة: 14 ✅

# اختبر 3: تحقق من الدروس مع علاقة Zoom
>>> Lesson::first()->zoomMeeting
# النتيجة المتوقعة: null (طبيعي إذا لم تضف بعد)

exit()
```

---

### 2️⃣ اختبر النموذج من الإدارة

```
1. ادخل: /admin
2. اذهب إلى: Courses → Course → Section → Edit Lesson
3. ابحث عن: "إضافة اجتماع Zoom"
4. يجب أن تجد:
   ☐ إضافة اجتماع Zoom
   
5. اضغط عليه لتفعيله:
   ☑ إضافة اجتماع Zoom
   
6. يجب أن تظهر حقول جديدة:
   ✅ موعد الاجتماع (Date Picker)
   ✅ مدة الاجتماع (Number)
   ✅ كلمة مرور (Text)
```

---

### 3️⃣ اختبر الحفظ

```
1. ملأ البيانات:
   - الموعد: 2026-02-15 14:30
   - المدة: 60
   - كلمة المرور: 123456

2. اضغط: Save

3. تحقق من النتيجة:
   ✅ يجب أن ترى رسالة نجاح
   ✅ يجب أن تعود إلى القائمة
   ✅ يجب أن تجد 📹 بجانب الدرس
```

---

### 4️⃣ اختبر البيانات في قاعدة البيانات

```bash
php artisan tinker

# عد الاجتماعات
>>> ZoomMeeting::count()
# النتيجة: 1 (أو أكثر إذا أضفت أكثر)

# اعرض الاجتماع الأول
>>> ZoomMeeting::first()
# يجب أن ترى البيانات المحفوظة

# تحقق من العلاقة
>>> $lesson = Lesson::find(2)
>>> $lesson->zoomMeeting
# يجب أن ترى بيانات الاجتماع

exit()
```

---

### 5️⃣ اختبر الحذف

```
1. ادخل درس له اجتماع Zoom
2. عطّل التبديل: ☐ إضافة اجتماع Zoom
3. اضغط Save
4. يجب أن يختفي الاجتماع
5. في الإدارة: 📹 يختفي من الجدول
6. في قاعدة البيانات: الصف يبقى لكن deleted_at ≠ null
```

---

### 6️⃣ اختبر السجلات

```bash
# اعرض آخر سطر في السجل
tail -20 storage/logs/laravel.log

# يجب أن ترى:
✅ لا توجد ERROR
✅ لا توجد Exception
✅ فقط INFO messages
```

---

## 🎯 قائمة التحقق الكاملة

| الاختبار | النتيجة المتوقعة | نتيجتك | ملاحظات |
|---------|-----------------|--------|---------|
| جدول موجود | ✅ true | | |
| 14 عمود | ✅ 14 | | |
| Form Fields تظهر | ✅ نعم | | |
| التبديل يعمل | ✅ نعم | | |
| الحقول تختفي عند التعطيل | ✅ نعم | | |
| الحفظ ينجح | ✅ نعم | | |
| البيانات محفوظة | ✅ نعم | | |
| العمود يظهر | ✅ 📹 | | |
| لا توجد أخطاء | ✅ 0 | | |

---

## ⚡ اختبار سريع جداً (30 ثانية)

```bash
# اختبر واحد: جدول موجود؟
php artisan tinker
>>> Schema::hasTable('zoom_meetings')

# يجب تحصل على: true ✅
```

**إذا حصلت على `true` ، كل شيء يعمل!** ✅

---

## 🔴 إذا شيء ما خطأ

### المشكلة: لا ترى حقول Zoom

**الحل:**
```bash
# 1. مسح الذاكرة
php artisan cache:clear
php artisan config:clear
php artisan view:clear

# 2. تحديث الصفحة في المتصفح
F5 (أو CTRL+SHIFT+DEL للتنظيف الشامل)

# 3. تحقق من Migration
php artisan migrate:status | grep zoom
```

### المشكلة: Migration لم ينفذ

**الحل:**
```bash
php artisan migrate
```

### المشكلة: خطأ في الحفظ

**الحل:**
```bash
# عرض الخطأ
tail -50 storage/logs/laravel.log

# تحقق من Validation
# تأكد من:
# - التاريخ والوقت بالصيغة الصحيحة
# - المدة رقم بين 15-480
# - التبديل مفعّل
```

---

## 📊 الإحصائيات المتوقعة

بعد الاختبار الناجح:

```
✅ Migration: 1 (تم تنفيذه)
✅ Form Fields: 4 (موجودة)
✅ Table Columns: 1 (موجود)
✅ Database Records: 0 أو أكثر (حسب الإضافات)
✅ Errors: 0 (لا توجد أخطاء)
```

---

## 🎓 ما تتوقع أن ترى

### في لوحة التحكم:
```
✅ حقول Zoom في نموذج الدرس
✅ عمود Zoom في جدول الدروس
✅ رموز 📹 للدروس ذات الاجتماعات
✅ ألوان مختلفة حسب الحالة
```

### في قاعدة البيانات:
```
✅ جدول zoom_meetings بـ 14 عمود
✅ Records محفوظة عند الإضافة
✅ Records محذوفة عند الحذف (soft delete)
```

### في السجلات:
```
✅ لا توجد error messages
✅ فقط INFO و DEBUG messages
✅ عمليات الحفظ والحذف موثقة
```

---

## 🏁 النتيجة النهائية

إذا نجحت جميع الاختبارات:

```
✅ النظام يعمل بشكل مثالي
✅ يمكنك الاستخدام فوراً
✅ البيانات محفوظة بأمان
✅ كل شيء جاهز للإنتاج
```

---

**تم:** 30 يناير 2026  
**النتيجة المتوقعة:** ✅ كل شيء يعمل!
